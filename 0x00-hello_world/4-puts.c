#include <stdio.h>

/**
 * main - a function that print a string for user using a puts function.
 * Return: 0 (Success).
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
